import type { w3xpConfig } from '@w3xp/ui'

const config: w3xpConfig = {
  title: "{{title}}",
  description: "{{description}}",
  network: {{network}},
  address: "{{address}}",
  verified: {{verified}}
}

export default config
